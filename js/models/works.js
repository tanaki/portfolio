// Filename: models/works
define([
	'Underscore',
	'Backbone'
], function(_, Backbone) {
	
	var worksModel = Backbone.Model.extend({
		defaults: {
			
		},
		initialize: function(){

		}
	});
	
	return worksModel;

});
